[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[project]
name = "trendsapi"
version = "1.0.2"
description = "Official Python client for the Trends API. Normalized 0-100 trend scores across Google, TikTok, Amazon, Reddit, YouTube, Steam, npm and 30+ sources. Sync + async, typed responses, free tier."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.9"
authors = [{ name = "Trends API", email = "hello@trendsapi.ai" }]
keywords = [
  "trends",
  "trends-api",
  "google-trends",
  "tiktok-trends",
  "amazon-trends",
  "reddit-trends",
  "youtube-trends",
  "pytrends",
  "pytrends-alternative",
  "keyword-research",
  "seo",
  "market-research",
  "mcp",
  "alternative-data",
]
classifiers = [
  "Development Status :: 5 - Production/Stable",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Internet :: WWW/HTTP",
  "Topic :: Software Development :: Libraries :: Python Modules",
  "Typing :: Typed",
]
dependencies = ["httpx>=0.27"]

[project.urls]
Homepage = "https://trendsapi.ai"
Documentation = "https://trendsapi.ai/#quickstart"
Repository = "https://github.com/trendsapi-ai/TrendsAPI-py"
"Bug Tracker" = "https://github.com/trendsapi-ai/TrendsAPI-py/issues"
"Get API key" = "https://trendsapi.ai/#get-key"

[tool.hatch.build.targets.wheel]
packages = ["src/trendsapi"]

[tool.hatch.build.targets.sdist]
include = ["src/trendsapi", "README.md", "LICENSE", "pyproject.toml", "examples"]
